import style from "./style.module.scss";

const ComingSoonSection = () => (
  <section className={style["prize-breakup-section"]}>
    <h4 className={style["prize-breakup-section-title"]}>
      {/* <span>PRIZE POOL</span> BREAKDOWN */}
      Something is being prepared exclusively for the community and customized
      specifically for its members.
    </h4>
    <p>COMING SOON</p>
  </section>
);

export default ComingSoonSection;
